//! LumaDB Admin UI (placeholder)
//!
//! In production, this would contain a Leptos-based admin dashboard.

pub struct AdminUI;

impl AdminUI {
    pub fn new() -> Self {
        Self
    }
}

impl Default for AdminUI {
    fn default() -> Self {
        Self::new()
    }
}
